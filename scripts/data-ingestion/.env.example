# Copy to .env and fill in. Loaded as environment variables by the scripts.
# Never commit .env.

# Included in the User-Agent so sources can reach us about our traffic.
CONTACT_EMAIL=you@example.com

# Minimum seconds between requests to the same host.
RATE_LIMIT_SECONDS=1.0
